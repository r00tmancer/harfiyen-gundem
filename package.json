{
  "name": "harfiyen",
  "private": true,
  "type": "module",
  "workspaces": ["shared", "worker", "web", "tools"],
  "scripts": {
    "dev:web": "npm run dev -w web",
    "dev:worker": "npm run dev -w worker",
    "build": "npm run build -w web",
    "typecheck": "npm run typecheck -w worker && npm run typecheck -w web",
    "test": "npm run test -w worker",
    "data": "node tools/build-wordlist.mjs",
    "deploy:worker": "npm run deploy -w worker",
    "deploy": "npm run build && npm run deploy:worker"
  }
}
